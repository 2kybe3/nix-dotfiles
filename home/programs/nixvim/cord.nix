{
  plugins.cord = {
    # TODO: upsteam broken too lazy to fix
    enable = false;
    settings = {
      display = {
        theme = "default";
        flavor = "dark";
      };
      editor = {
        client = "neovim";
        tooltip = "";
      };
    };
  };
}
